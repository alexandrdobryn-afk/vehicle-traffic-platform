# Optional experimental AI adapters.
#
# Keep these outside requirements.txt so the stable CPU backend can still build
# even when a research dependency changes its transitive packages.

# RF-DETR vehicle detector. Official package auto-downloads pretrained weights
# for RFDETRNano/RFDETRMedium on first use.
rfdetr==1.8.3

# PaddleOCR OCR engine. Useful for the Practical / Maximum Accuracy presets,
# but heavier than EasyOCR and may need PaddlePaddle-specific CPU/GPU wheels.
paddleocr==3.7.0
paddlepaddle==3.2.2

# Fast ALPR / ONNX OCR placeholder. The runtime adapter is intentionally marked
# experimental until a concrete package/artifact is selected and benchmarked.
fast-plate-ocr==1.1.0
fast-alpr[onnx]==0.4.0
open-image-models[onnx]==0.5.1
