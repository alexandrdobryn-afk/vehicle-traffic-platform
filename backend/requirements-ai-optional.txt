# Optional experimental AI adapters.
#
# Keep these outside requirements.txt so the stable CPU backend can still build
# even when a research dependency changes its transitive packages.

# RF-DETR object detector. Official package auto-downloads pretrained weights
# for RFDETRNano/RFDETRMedium on first use.
rfdetr==1.8.3

# Tracker adapters. ByteTrack/BoT-SORT come from Ultralytics; these packages
# enable the explicit DeepSORT and OC-SORT runtime choices.
deep-sort-realtime==1.3.2
filterpy==1.4.5

# Generic scene-text OCR candidates. Keep OCR optional until the text module is
# wired for signs, labels, building numbers and markings.
paddleocr==3.7.0
paddlepaddle==3.2.2
easyocr==1.7.2
